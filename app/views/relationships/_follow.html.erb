<%= form_with(model: current_user.active_relationships.build, local: true)  do |f| %>
  <div><%= hidden_field_tag :following_id, @user.id %></div>
  <%= f.submit "ともだち追加", class: "ui basic button" %>
  <i class="heart icon"></i>
<% end %>
